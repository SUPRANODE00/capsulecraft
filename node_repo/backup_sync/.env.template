# Pentacycle Origin Environment Configuration
# Instructions for collaborators:
# 1. Copy this file to .env (cp .env.template .env)
# 2. Generate your unique decryption key: python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# 3. Paste the resulting string below:

FERNET_KEY=your_generated_key_here
